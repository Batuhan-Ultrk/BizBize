import Foundation

enum APIEnvironment {
    // TODO: Replace with production/staging configuration when backend environments are finalized.
    static let baseURL = URL(string: "https://salvation-rec-celebration-impose.trycloudflare.com")!
}
